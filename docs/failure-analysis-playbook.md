# Failure Analysis Playbook

| # | Symptom | Likely Cause | Artifact to Inspect | Risk Level | Safe Operator Response |
|---:|---|---|---|---|---|
| 1 | Agent changed workflow file and stalled | Approval gate required for workflow changes | PR timeline, session log | High | Pause merge, request human approval, inspect changed workflow diff |
| 2 | Downstream job cannot find expected output | `$GITHUB_OUTPUT` name mismatch or missing job output | Workflow logs, workflow YAML | Medium | Fix output key, rerun workflow, verify producing job artifact |
| 3 | MCP tool unavailable | Tool not listed or server config mismatch | Agent file, MCP config | Medium | Confirm server name/tool name, narrow allowlist, retry after review |
| 4 | Broad MCP access appears in agent profile | Wildcard allowlist such as `server/*` | `.agent.md`, MCP config | High | Replace wildcard with explicit tools, require security review |
| 5 | Agent resumed stale session | Session state older than branch changes | Session log, memory file | Medium | Reset or re-scope session, validate current branch state |
| 6 | Reviewer and builder disagree on output | Conflicting task interpretation or stale artifact | Reviewer findings, builder summary | Medium | Consolidate conflicts, require planner clarification |
| 7 | Artifact deleted before audit review | Retention or manual deletion issue | Audit log, workflow artifacts | High | Block final approval, regenerate artifact, preserve deletion record |
| 8 | Hook blocked execute access | Pre-tool policy denied command execution | Hook event log | Medium | Confirm deny was expected, escalate only if execution is approved |
| 9 | Memory fact conflicts with current repo | Stale repository fact or invalid citation | Memory files, current code | Medium | Prune stale memory, regenerate validated repository facts |
| 10 | Security gate failed due to permissions | Workflow lacks required permission or has unsafe permission | Security gate logs, workflow YAML | High | Correct permissions, rerun gate, avoid broad permissions unless justified |
| 11 | Branch scope mismatch | Agent ran on default branch or wrong PR branch | Workflow context, session log | High | Stop action, re-run on correct branch/ref, verify no unintended writes |
| 12 | Human approval skipped for high-risk action | Missing approval condition or weak workflow gate | PR timeline, workflow YAML | Critical | Revert if needed, add explicit approval gate, conduct audit |
| 13 | Final report omits security finding | Consolidator consumed wrong or stale artifact | Consolidated report, audit artifact | High | Block final decision, regenerate report from current inputs |
| 14 | Matrix job hides partial failure | One axis failed but final job ignored status | Workflow logs, matrix strategy | Medium | Add explicit needs/status checks and artifact validation |
| 15 | Agent repeats previously completed work | Missing durable state or failed resume detection | Session log, planner artifact | Low/Medium | Reconcile state, update progress artifact, resume from last verified step |
| 16 | Remote MCP endpoint used without trust notes | External tool boundary undocumented | MCP config, governance review | High | Require trust review, document data flow, narrow or disable access |
| 17 | Planner artifact missing required success criteria | Planner skipped a required field or schema validation was not enforced | `artifacts/planner-output.json`, `schemas/planner-output.schema.json` | Medium | Block execution, regenerate the planner artifact, and verify success criteria before handoff |
| 18 | Consolidated report missing unresolved security findings | Consolidator consumed stale inputs or omitted security audit findings | `artifacts/consolidated-operator-report.json`, `artifacts/security-audit-report.json` | High | Block final approval, regenerate the consolidated report from current artifacts, and require operator review |
| 19 | Reviewer note leaked into final artifact | Draft editorial note was copied into published documentation or PR output | Final docs, PR description, review artifact | Medium | Block publish or merge, remove the note, preserve evidence, and add final-render review |
| 20 | Lab answer key contradicts source artifact | Answer key was updated without rechecking the referenced artifact | Lab file, referenced artifact, README lab index | Medium | Pause release, correct the answer key, and review the affected lab against source evidence |
| 21 | Schema validates but artifact content is misleading | Required fields are present but the content omits or misstates important risk | Artifact JSON, matching schema, upstream evidence | High | Continue human review, compare against source artifacts, and regenerate misleading content |
| 22 | Downstream job consumes stale artifact name | Output or upload name changed without updating the downstream download | Workflow YAML, workflow logs, artifact list | Medium | Fix the producer/consumer name mismatch, keep gating enabled, and rerun the workflow |
| 23 | Valid JSON fails expected handoff schema | Upstream agent changed field names or omitted required handoff fields | `artifacts/planner-context-raw-malformed.json`, `schemas/agent-handoff-v2.schema.json` | High | Mark artifact invalid, block downstream invocation, and regenerate with the expected schema |
| 24 | Downstream agent processes zero files but reports success | Missing or drifted file list caused silent no-op behavior | Handoff artifact, builder summary, workflow logs | High | Treat as silent failure, block the report, and require a validated handoff with non-empty files when files are expected |
| 25 | Oversized artifact risks context-window failure | Raw logs or broad diffs exceed the downstream context budget | Handoff artifact, context budget, source logs | Medium/High | Reject oversized input, require scoped summaries with source links, and preserve unresolved risks |
| 26 | Agent repeatedly retries missing artifact download | Retry loop lacks a stop condition or explicit missing-artifact state | Workflow logs, retry annotations, artifact list | Medium | Stop after max retries, surface the missing artifact, and route to operator review |
| 27 | Final report summarizes missing context as if it were verified | Consolidator trusted incomplete or stale handoff context | Final report, upstream handoff, validation output | High | Block final approval, preserve evidence, and regenerate from validated upstream artifacts |
| 28 | Workflow succeeds but agent processed zero files | Gate checked job completion but not meaningful handoff content | Workflow logs, handoff artifact, builder summary | High | Treat as silent failure, require non-empty expected file processing, and regenerate the handoff |
| 29 | Downstream agent guesses missing artifact content | Missing artifact was summarized or inferred instead of blocked | Workflow logs, agent output, artifact list | High | Fail closed, preserve the missing-artifact evidence, and rerun only after the artifact exists |
| 30 | Risky educational example triggers active security gate failure | Gate scans inert lab examples as if they were active configuration | `mcp/`, `.github/agents/`, `.github/workflows/security-gate.yml` | Medium | Distinguish educational examples from active config, warn on examples, and fail on active risk |
| 31 | Workflow gate passes because it checks file existence but not schema validity | Artifact exists but is malformed, stale, or missing required fields | Workflow YAML, artifact JSON, schema file | High | Add schema validation before downstream agent invocation |
| 32 | Final report generated from stale artifact | Consolidator consumed prior-run evidence or unresolved risks from an old branch | Final report, artifact timestamps, commit SHA, run ID | High | Block final approval and regenerate from current branch/run artifacts |
| 33 | Hook blocks command but workflow records success | Hook denial was logged but not propagated to workflow failure state | Hook event, workflow logs, final report | High | Treat deny as blocking, fail the workflow, and preserve hook evidence |
| 34 | Context-window truncation hides unresolved risks | Oversized artifact or compressed summary dropped risk details | Context budget, source artifact, final summary | Medium/High | Reject or chunk oversized artifacts and require summaries to link to source evidence |
| 35 | Agent profile change grants execute access without review | `.agent.md` profile changed tool authority but bypassed human control-plane review | `.github/agents/`, agent profile review report, PR diff | Critical | Block merge, require human review, and verify deterministic approval gates before execution |
| 36 | Reviewer role dynamically escalates into executor role | Workflow or runner allowed role transition based on model reasoning instead of policy | Agent profiles, runner workflow, session log | High | Block the run and require explicit workflow-enforced role separation |
| 37 | Natural-language safety rule lacks deterministic enforcement | Policy exists only as prose without hook, workflow, allowlist, or approval gate | Guardrail docs, hooks, workflow policy, agent profile | High | Add deterministic enforcement before allowing high-risk tool calls |
| 38 | Agent profile consumes untrusted PR content before privileged tool use | Executor context mixed untrusted PR or issue text with high-authority operating instructions | Executor profile, PR comments, workflow logs, handoff artifacts | High | Sanitize and classify inputs, block privileged use, and preserve contamination evidence |
| 39 | Builder receives empty `plan_artifact_name` | Planner output key was missing, blank, or failed to serialize state | Workflow logs, planner state manifest, `$GITHUB_OUTPUT` step | High | Fail closed before builder invocation and regenerate planner state |
| 40 | Artifact name contains unexpected whitespace or markdown formatting | Planner produced a human-formatted name instead of a deterministic machine contract | Workflow output, artifact list, planner state manifest | Medium/High | Reject malformed names, normalize at the producer, and rerun validation |
| 41 | Builder consumes stale planner artifact from previous run | Fallback cache behavior used old planner context after current state was missing | Stale cache artifact, run ID, commit SHA, source branch | High | Block stale fallback unless explicitly approved and labeled as previous-run context |
| 42 | Downstream agent enters retry loop after missing artifact | Missing state triggered repeated downloads, regeneration, or tool calls | Workflow logs, retry annotations, cost controls | Medium/High | Stop after max retries, surface explicit failure state, and preserve cost/rate-limit evidence |
| 43 | Workflow passes even though Builder processed zero valid files | Gate checked job completion but not validated state or processed file count | Builder summary, planner manifest, workflow logs | High | Treat as silent success, block final reporting, and require validated non-empty state |
| 44 | Final report generated from unverified planner state | Consolidator trusted a report chain without current planner state provenance | Final report, planner manifest, consolidated workflow | High | Block final approval and regenerate from verified current-run planner state |
| 45 | Agent profile grants wildcard MCP access | Tool allowlist uses a server wildcard instead of reviewed tool names | Agent profile, MCP config, MCP boundary review report | High | Block or narrow to explicit tools, document justification, and require human review |
| 46 | MCP server adds new tool after approval | Runtime tool list changed after earlier review, including possible server-declared `listChanged` behavior | Server manifest snapshot, MCP boundary review report, run log | High | Revalidate discovered tools, block wildcard access, and preserve runtime tool-list evidence |
| 47 | Remote MCP server receives repository context without review | Remote tool boundary, ownership, auth, logging, or data egress was not documented | MCP config, governance doc, tool-call log | High | Pause use, document data flow and owner, narrow access, and require approval |
| 48 | Downstream agent treats MCP output as verified truth | Tool response was consumed without schema, source, timestamp, or provenance validation | Tool output artifact, downstream report, MCP boundary review report | High | Mark output as evidence only, validate provenance, and regenerate downstream conclusions |
| 49 | Local stdio MCP server expands host process authority | Stdio subprocess boundary exposes broader local capabilities than the agent role requires | Local MCP config, server review notes, agent profile | High | Require trusted config, isolation review, explicit tools, and human approval |
| 50 | MCP config changed without human review | Control-plane file changed tool authority outside the review path | MCP config diff, PR review, CODEOWNERS, workflow gate | Critical | Block merge, restore review gate, and require approval before enabling the boundary |
| 51 | Resumed session continues on stale branch context | Session state was rehydrated without comparing current branch to the original session branch | Session log, git state snapshot, session continuity report | High | Block privileged work, verify branch provenance, and reset or re-scope the session if branch state drifted |
| 52 | Session ID matches but commit SHA changed | Out-of-band repository change occurred while the agent was suspended | Git state snapshot, session continuity report, PR timeline | High | Inspect the intervening diff, regenerate stale artifacts, and require approval before continuation |
| 53 | Human hotfix lands while agent session is suspended | Repository state moved forward outside the agent's remembered plan | Git state snapshot, branch history, planner artifact | High | Treat the old plan as stale, preserve evidence, and revalidate against current files before editing |
| 54 | Previous run crashed but resumed session treats it as completed | Failure footprint was carried forward without recovery validation | Cloud session log, continuity report, workflow logs | High | Mark prior state unresolved, inspect crash or stall evidence, and rerun only from verified state |
| 55 | Old error trace is replayed into current execution | Resumed context anchored the agent to a failure that may no longer exist | Workflow permission log, session log, current workflow YAML | Medium/High | Confirm the error is still current before applying fixes or escalating permissions |
| 56 | Tool permissions changed between original and resumed session | Agent profile, MCP config, hook, or workflow permission changed during suspension | Agent profiles, MCP configs, hooks, continuity report | Critical | Block tool use until permission changes are reviewed and approved |
| 57 | Memory fact validated in old run but not current run | Memory validation timestamp or TTL was reused as if it applied to the new run | Memory files, session continuity report, current source artifact | High | Revalidate memory against current repository state and mark stale facts invalid |
| 58 | Resumed agent performs privileged action before continuity review | Workflow or operator allowed action before branch, commit, memory, permissions, and approvals were checked | Session continuity report, workflow logs, hook logs | Critical | Stop the run, preserve evidence, and require deterministic continuity validation before retry |
| 59 | Hook logs warning but does not block tool execution | Advisory event was mistaken for enforced policy | Hook event, hook enforcement report, workflow logs | High | Treat the control as advisory, add pre-tool enforcement, and fail closed for high-risk calls |
| 60 | `postToolUse` hook detects risk after action already completed | Detection occurred after execution instead of before tool invocation | Post-tool hook event, tool result, audit log | High | Preserve evidence, add pre-tool mediation, and block future calls before execution |
| 61 | Hook policy unavailable and workflow fails open | Missing, crashed, or ambiguous policy allowed execution to proceed | Workflow logs, hook enforcement report, runner policy | Critical | Change policy behavior to fail closed and block privileged execution until policy is available |
| 62 | Hook file changed without required review | Control-plane guardrail was weakened or removed by normal file change | Hook diff, CODEOWNERS, PR review, hook enforcement report | Critical | Block merge, restore protected hook policy, and require human review |
| 63 | CLI-only permission request mistaken for cloud enforcement | Local permission prompt was treated as proof that all runtimes enforce the same control | Permission request artifact, cloud workflow logs, runner policy | High | Verify enforcement plane, add cloud-side pre-tool checks, and document scope limits |
| 64 | Hook blocks command but workflow records success | Hook denial was not propagated to workflow or agent-runner status | Hook event, workflow logs, final report | High | Fail the workflow, preserve denial evidence, and make blocked calls blocking status |
| 65 | Alternate workflow path bypasses hook enforcement | A second runner path invoked tools without the same pre-tool policy | Workflow files, runner config, hook enforcement report | Critical | Require centralized enforcement and review all execution paths before privileged use |
| 66 | Hook only matches raw strings and misses equivalent capability intent | Policy relied on brittle text matching instead of semantic capability analysis | Intent analysis payload, hook policy, blocked/allowed call evidence | High | Add capability, target, scope, and risk analysis before decision |
| 67 | Approval token is reused for a different operation | Human approval was broad, stale, or not bound to the specific payload | Approval binding report, PR comments, run log | Critical | Invalidate the approval and bind future approvals to run ID, branch, commit SHA, payload hash, and expiration |
| 68 | Payload changes after approval but before execution | Time-of-check to time-of-use gap allowed approved evidence to drift | Approval binding report, payload hash, workflow log | Critical | Block execution, require a new review, and re-check immutable evidence immediately before use |
| 69 | Memory fact lacks source commit | Memory fact cannot be tied to the repository state that produced it | Memory validation report, repository facts, memory governance doc | High | Treat as untrusted, verify against current files, and quarantine before context injection |
| 70 | Memory fact conflicts with agent profile authority | Stored memory claims an agent can perform an action its `.agent.md` profile does not allow | Memory authority review report, builder/executor profiles | Critical | Trust current agent profile, block the action, and route through an authorized profile with approval |
| 71 | Builder Agent attempts self-remediation for missing execution authority | Agent tries to bridge a permission gap by editing workflow, helper files, or memory | Builder profile, memory authority review report, PR diff | Critical | Block self-remediation and require human-reviewed control-plane change or Executor handoff |
| 72 | Vector index not regenerated after repository change | Long-term memory index was generated from an older branch or commit | Memory vector index lock, repository facts, current branch state | High | Exclude stale index, schedule re-indexing, and preserve invalidation evidence |
| 73 | Agent writes back to its own memory without review | Memory write path lets the agent update future context after its own reasoning | Memory files, PR diff, memory governance doc | Critical | Require review for memory writes and quarantine authority-affecting updates |
| 74 | Repository evidence conflicts with memory fact | Current source files disagree with remembered repository state | Current file, memory validation report, repository facts | High | Trust source-controlled evidence and mark conflicting memory stale or invalid |
| 75 | Stale memory enters context window before validation | Runtime loaded old or unpinned memory before pre-flight checks | Session log, memory validation report, memory index lock | High | Stop the run, remove stale context, and validate memory before retry |
| 76 | Memory update changes scope or authority without human review | Memory mutation affects role, tool, workflow, or policy interpretation | Memory diff, agent profiles, workflow policy, review history | Critical | Block merge, require human review, and regenerate memory from approved sources |
| 77 | Consolidator drops conditional approval language | Final report compresses a conditional review into clean approval or vague readiness | Consolidation fidelity report, reviewer findings, final report | High | Block final approval and regenerate with conditional language preserved |
| 78 | Security finding downgraded during summary | Final report rewrites required approval or risk into mild warning language | Security audit report, consolidation fidelity report, final report | Critical | Restore original severity and require human escalation before approval |
| 79 | Reviewer relies on builder summary instead of raw evidence | Downstream review inherits Builder assumptions or omissions | Builder summary, raw diff, reviewer findings, multi-agent manifest | High | Require reviewer to inspect primary evidence or preserve scope limitation |
| 80 | Multi-agent consensus mistaken for correctness | Sequential agents agree because each inherited the same unverified premise | Multi-agent manifest, planner output, reviewer findings | High | Require independent evidence checks and preserve dissent or assumptions |
| 81 | Final report generated from file existence checks only | Workflow checks that files exist but not schema validity, freshness, provenance, or semantic fidelity | Consolidated workflow, schema files, consolidation fidelity report | High | Add schema and semantic fidelity gates before publication |
| 82 | Provenance metadata dropped during JSON merge | Consolidation loses run ID, branch, commit SHA, timestamps, or source artifact names | Multi-agent manifest, final report, artifact schema map | High | Preserve chain-of-custody fields and regenerate report from current artifacts |
| 83 | Dissent item omitted from consolidated report | Minority reviewer or security concern disappears from final summary | Multi-agent manifest, reviewer findings, consolidated report | High | Treat omission as blocking and require explicit dissent preservation |
| 84 | Critical or unresolved finding fails to force escalation | Global status is approved despite unresolved or conditional upstream risk | Multi-agent manifest, consolidation rules, final report | Critical | Apply deterministic merge rules and escalate to human review |
| 85 | Raw LLM response buffer written to public markdown | Runner treated the whole model response as a final artifact instead of extracting an approved public field | Publication readiness report, generated docs, workflow logs | High | Block publication and regenerate from `final_output` or another approved public payload field |
| 86 | Reviewer scratchpad field copied into final artifact | Orchestration selected an internal field or concatenated internal and public fields | Reviewer scratchpad, leakage report, generated docs | High | Block publication, preserve evidence, and enforce field-level serialization |
| 87 | Hidden instruction placeholder appears in public document | Internal placeholder was not stripped before public artifact promotion | Public docs, leakage report, publication gate | High | Remove the artifact from promotion, regenerate from public payload, and add a placeholder scan |
| 88 | Private context reference appears in public document | Agent used private review context and the runner wrote the wrong output field | Scratchpad, source payload, generated docs | Critical | Block release, preserve the affected artifact, and require human review before regeneration |
| 89 | Markdown syntax check passes despite internal note leakage | Gate validated formatting but not semantic publication readiness | Markdown lint output, leakage report, promotion workflow | Medium/High | Add leakage checks and structured output validation before publication |
| 90 | Publication gate checks file existence but not leakage state | Workflow only verified that docs or JSON files were present | Publish workflow, leakage report, readiness report | High | Fail closed unless leakage status and promotion status are approved |
| 91 | Human override publishes artifact with unresolved comments | Override path bypassed review-note and unresolved-comment checks | PR timeline, publication readiness report, approval record | Critical | Require explicit human approval tied to run ID, branch, commit SHA, and unresolved findings |
| 92 | Final artifact generated from internal field instead of public payload field | Structured output parser selected `simulated_internal_notes` or metadata rather than `final_output` | Reviewer scratchpad, schema, runner logs | High | Reject the artifact and enforce an allowlist of public payload fields |
| 93 | Schema-valid artifact contains generic filler | Agent satisfied required fields with phrases such as "Looks good" or "Make sure it works" | Schema semantic validation report, planner output, validation telemetry | Medium/High | Treat as semantic validation failure and require specific evidence before downstream use |
| 94 | Required field present but semantically empty | Required string or array contains placeholder-style content such as "None" | Artifact JSON, matching schema, semantic validation report | High | Block automation and regenerate the field from current source evidence |
| 95 | Consolidated report says no unresolved risks despite upstream findings | Consolidator compressed or omitted upstream risk language | Consolidated report, reviewer findings, security audit report | High | Fail closed, preserve conflict evidence, and require human review |
| 96 | Complex security caveat omitted because schema had no field for it | Schema over-optimized for simple status values and lost conditional nuance | Schema file, upstream report, consolidated report | High | Extend the contract or add an evidence field before accepting the report |
| 97 | Error object coerced into generic string | Agent converted structured error or caveat into vague text to satisfy schema type | Validation telemetry, artifact JSON, producer logs | Medium/High | Reject generic string coercion and preserve structured error evidence |
| 98 | Downstream agent trusts schema-valid artifact without semantic checks | Workflow treats schema pass as decision-ready approval | Workflow logs, semantic validation report, downstream output | High | Add semantic, provenance, and cross-artifact gates before invocation |
| 99 | Schema validation passes but provenance or run context is missing | Artifact shape is valid but cannot be tied to current run, branch, or commit | Artifact JSON, workflow run, schema map | High | Block downstream use until provenance metadata is present and current |
| 100 | Validation telemetry shows structural pass and semantic block | Pipeline recorded schema success but semantic checks found conflict or filler | `artifacts/validation-telemetry.log`, schema semantic validation report | High | Respect the semantic block and require review before rerun |
| 101 | Runner step succeeds but agent runtime incomplete | GitHub Actions step exited successfully while internal agent work remained unfinished | Pipeline execution checkpoint, workflow logs | High | Block upload and downstream consumption until runtime status is complete |
| 102 | Artifact upload races file generation | Upload begins before the producing task finishes writing all records | Artifact transaction report, pipeline checkpoint | High | Fail closed, regenerate the artifact, and upload only after transaction checks pass |
| 103 | Artifact exists but is empty or truncated | Gate checked file existence but not size, record count, or write completion | Artifact transaction report, uploaded artifact, workflow logs | High | Reject the artifact and require file-size and record-count validation |
| 104 | Artifact parses but contains fewer records than expected | Partial file is syntactically valid but incomplete | Artifact transaction report, validation telemetry | High | Block downstream agents and regenerate from the producing job |
| 105 | Downstream job processes zero files | Builder or reviewer booted from missing, empty, or mismatched handoff state | Builder summary, workflow logs, provenance manifest | High | Treat as silent failure and block final reporting |
| 106 | `needs` passes but artifact provenance is wrong | Job ordering succeeded but consumed artifact came from wrong run, branch, commit, or producer | Artifact provenance manifest, workflow run metadata | High | Block downstream use until provenance matches expected run evidence |
| 107 | Artifact name collision causes stale download | Reused artifact name or retention behavior resolved previous-run state | Provenance manifest, artifact list, workflow logs | High | Bind downloads to run, attempt, branch, commit, producing job, and fake digest evidence |
| 108 | Final report generated from wrong-run artifact | Consolidator consumed stale or mismatched upstream evidence | Final report, provenance manifest, transaction report | Critical | Invalidate the final report and regenerate from current-run artifacts |
| 109 | Upload gate checks existence but not transaction readiness | Workflow uploads a file before parse, schema, semantic, record-count, or digest checks | Upload workflow, artifact transaction report | High | Add transaction readiness checks before upload |
| 110 | Downstream agent consumes artifact before semantic validation | Workflow invokes agent after parse/schema pass but before content integrity review | Semantic validation report, transaction report, downstream output | High | Fail closed until semantic validation and provenance checks pass |
| 111 | Pipeline green but downstream processed zero items | Runner status was treated as success even though agentic work did not happen | Zombie pipeline failure report, builder summary, workflow logs | Critical | Block final approval and require a processed-item assertion before downstream use |
| 112 | Schema drift causes silent zero-work success | Downstream agent ignored drifted fields and defaulted to empty work | Malformed handoff, compound failure report, schema file | High | Reject the handoff, quarantine drifted fields, and regenerate from the expected schema |
| 113 | Unexpected upstream JSON keys enter downstream prompt context | Unknown fields were passed into agent context instead of rejected or quarantined | Compound failure report, handoff artifact, schema | High | Reject unknown keys or quarantine the artifact before invocation |
| 114 | Retry loop grows token usage without semantic progress | Agent retries after deterministic handoff failure with larger context each time | Billing and token telemetry, cost control doc | High | Stop the chain, enforce retry/token budgets, and require human review |
| 115 | Context window bloats with repeated failure traces | Retry attempts append logs and stale summaries until source evidence is crowded out | Context window gate, telemetry, agent log | Medium/High | Block bloated reruns and restart from scoped source evidence |
| 116 | Final report hides collapsed agentic state chain | Consolidator summarizes green status while omitting zero-work and blocked semantic findings | Zombie pipeline report, compound failure report, final report | Critical | Invalidate the report and regenerate with all blocking findings preserved |
| 117 | Human approves based on polished but incomplete report | Approval gate reviewed formatting rather than full chain-of-trust evidence | PR timeline, final report, compound failure report | Critical | Revoke approval path and require complete evidence review |
| 118 | Artifact provenance incomplete during handoff | Artifact cannot be tied to run, branch, commit, workflow, job, agent, or digest | Provenance manifest, transaction report | High | Block downstream consumption until provenance is complete and current |
| 119 | Schema validation passes but semantic assertions fail | Required fields exist but processed counts, risk preservation, or specificity checks fail | Semantic validation report, zombie pipeline report | High | Treat semantic failure as blocking despite schema success |
| 120 | Multiple control-plane layers fail without a global block | Schema, memory, provenance, tool, hook, retry, and consolidation checks are evaluated independently | Compound handoff failure report, governance maps | Critical | Apply deterministic global block and escalate to a human operator |
